# Load local environment variables from .envrc.local
# This file is committed to the repo, while .envrc.local is gitignored
# Create .envrc.local from the template:
#   cp .envrc.local.example .envrc.local

# Load .envrc.local if it exists
source_env_if_exists .envrc.local

# Enable nix-shell integration (if using direnv with nix)
# use nix

# Note: After creating/editing .envrc.local, run: direnv allow
